list yang di install
- venv
- pip install Flask
- pip install connexion[swagger-ui]
- pip install Flask-SQLAlchemy flask-marshmallow marshmallow-sqlalchemy marshmallow


sama kek sesi sebelumnya, ditambah ini
    "pip install Flask-SQLAlchemy flask-marshmallow marshmallow-sqlalchemy marshmallow"

copas people.db, models, config, build_database dari source di git (https://github.com/ardhiraka/H8Flask/tree/master/version2)

build_db buat bikin .db (kaya import db di c#)